# NOM Nowcast — project status

A snapshot of where the project sits and what's worth doing next.
Keep this in sync as work lands; it's the fastest way to pick the
project back up after a break.

## Where it lives

- Repo: <https://github.com/DavidAStephan/NOM_Nowcast>
- Site: <https://davidastephan.github.io/NOM_Nowcast/>
- Build: GitHub Pages, rebuilt on every push to `main` and weekly
  on a Monday morning cron via `.github/workflows/render.yml`.

## Current production model

The landing-page headline is the **`bayes_kalman`** Bayesian SSM
(Phase 3 v0.6): same trivariate observation block as `bayes_gamma`
(OAD long-term arrivals, lagged DHA visa grants, ABS NOM) but the
latent state trajectory is integrated out via a Kalman filter
inside Stan, so only ~10 static parameters are sampled. The
calibration analysis (Phase 3 v0.8, see backtest.qmd) showed
`bayes_kalman` is the only model whose 80% / 95% credible intervals
come close to their nominal coverage in the post-COVID period;
`bayes_gamma` and the production Kalman are significantly
over-confident there.

The headline reads from a snapshot at `data/headline_bayes.rds`
generated by `scripts/refresh_headline_bayes.R`. The Stan compile
and HMC sampling happen locally (or via the `Refresh headline
(Bayes)` GitHub Action); the render workflow itself stays
cmdstanr-free.

## Phase progression

| Phase | What | Status |
|-------|------|--------|
| Phase 1 | Per-category univariate Kalman + empirical-π aggregation | Done |
| Phase 2 v1.0 | Multivariate Kalman: OAD + visa on a shared latent | Done |
| Phase 2 v2.0 | Add NOM as a third observation row | Done |
| Phase 2 v3.0 | Time-varying $\alpha_n$ (negative result, flagged off) | Done |
| Phase 2 v4.0 | Parametric Gamma lag via state augmentation | Done |
| Phase 2 v5.0 | Grid-search $(\alpha, \beta)$ by panel logLik | Done |
| Phase 3 v0.1 | `bayes_headline` — port to Stan | Done |
| Phase 3 v0.2 | `bayes_gamma` — Gamma lag in Bayes | Done |
| Phase 3 v0.3 | `bayes_gamma_learned` — sample $(\alpha, \beta)$ | Done |
| Phase 3 v0.4 | Promote bayes_gamma to landing-page headline | Done; superseded by v0.9 |
| Phase 3 v0.5 | HMC diagnostics cleanup (tighter sampling config) | Done |
| Phase 3 v0.6 | `bayes_kalman` — Kalman marginal likelihood | Done; **production headline** |
| Phase 3 v0.7 | RTS smoother | Negative result, reverted |
| Phase 3 v0.8 | Calibration check + log score on backtest page | Done |
| Phase 3 v0.9 | Promote bayes_kalman to headline (calibration win) | Done |

## Backtest leaderboard (post-COVID)

| Model | h=-1 RMSE | h=0 RMSE | h=0 log-score | h=0 cov80 |
|-------|-----------|----------|---------------|-----------|
| `kalman_multi_v2` (Phase 2 v4.0) | 36.7k | 77.9k | -26,839 | 0% |
| `bayes_headline` (Phase 3 v0.1) | 47.3k | 67.0k | — | — |
| `bayes_gamma` (Phase 3 v0.2) | 46.9k | 63.8k | -39 | 18% |
| `bayes_gamma_learned` (Phase 3 v0.3) | 46.7k | 65.4k | — | — |
| `bayes_kalman` (Phase 3 v0.6, current headline) | **34.0k** | 64.9k | **-14** | **45%** |

`bayes_kalman` was promoted to the headline (v0.9) after the
calibration analysis showed it's the only model whose 80% / 95%
credible intervals come close to nominal coverage. Point RMSE at
$h = 0$ is essentially tied with `bayes_gamma` (1k difference); on
the proper-scoring-rule log-score it dominates by a large margin.

## Open follow-ups

In rough priority order:

1. **Sync the `renv` lockfile.** Several Phase 3 deps (cmdstanr,
   posterior, distributional, abind, matrixStats, tensorA,
   checkmate) were added without an `renv::snapshot()`. The local
   shells complain "project is out-of-sync".
2. **Phase 3 v0.8 — forward-lag `bayes_kalman`.** The current
   Kalman model processes visa with a backward lag because that's
   what a causal forward filter natively supports. A trailing-block
   pass over visa observations (after the main filter completes)
   would let it adopt the forward-lag semantics of `bayes_gamma`
   and might close the small $h = 0$ gap. Moderate Stan work.
3. **Fix the students data pipeline.** Department of Education
   moved most of the international-student data to a Power BI
   dashboard; the existing scraper of `education.gov.au/...` hits
   HTTP/2 framing errors and the panel currently ships with
   `empty_students_clean()`. Restoring the third upstream signal
   would help category-level disaggregation.
4. **Per-category hierarchical Bayes.** Earlier attempt found the
   per-category OAD series don't compose to the total (different
   visa-class definitions), so summed forecasts inflate by ~10×.
   Would need data-side restructuring to identify a clean partition
   before retrying.

## Refresh cadence

- **`bayes_kalman` headline:** `scripts/refresh_headline_bayes.R`
  locally, or the `Refresh headline (Bayes)` workflow (Mondays).
  Each fit is ~60-90s.
- **Backtest snapshot:** `NN_REFRESH_BAYES=1 Rscript
  scripts/refresh_backtest.R` locally. The full grid with 4 Bayes
  variants × 12 quarters ≈ 35 minutes. There's also a `Refresh
  backtest` workflow that runs the non-Bayes grid monthly.

## Surprising things to remember

- HMC's preferred Gamma lag is much earlier than the Kalman v5 grid
  searched over ($\alpha \approx 0.85$, $\beta \approx 3.4$). The
  `bayes_gamma_learned` posterior puts $\sim 83\%$ of the visa-to-
  arrivals weight on the *current* quarter. The kalman v5 box
  ($\alpha \in [1, 4]$) missed this region entirely.
- The RTS smoother made backtest RMSE *worse* than the filter (v0.7
  negative result). The model has a small downward bias the smoother
  amplifies across time.
- ABS NOM is published with a $\sim 6$-month lag; the visa-grants
  CKAN feed is more current and is the only forward-looking signal
  in the model.
